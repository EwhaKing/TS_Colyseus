let person = {
    name : "지현",
    age : 23,
    email : "micekj0904@ewhain.net"
};

console.log(`이름: ${person.name}`);
console.log(`나이: ${person.age}`);
if (person.email) {
  console.log(`이메일: ${person.email}`);
}
