// `4-함수선언.ts`
// 두 숫자를 입력받아 합을 반환하는 함수 선언 (매개변수 타입, 반환 타입 명시)
// 함수를 호출해서 결과 출력

function addition(x: number, y: number) : number {
    return x + y;
}
// function 함수 이름(변수: 타입, 변수: 타입) : return 타입 { 함수 content }

console.log(addition(3,25));