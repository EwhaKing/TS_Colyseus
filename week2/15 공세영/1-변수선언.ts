// 이름 변수는 string으로 지정. 값은 내 이름이다.
let 이름: string = "공세영";

// 나이 변수는 number로 지정. 숫자니까.
let 나이: number = 22;

// 합격 여부는 true/false로 나뉘는 boolean으로 지정.
let 합격여부: boolean = true;


// 아래로는 콘솔창에 위 세 가지 변수를 출력하는 코드.
console.log(이름);
console.log(나이);
console.log(합격여부);